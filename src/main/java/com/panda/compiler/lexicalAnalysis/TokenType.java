package com.panda.compiler.lexicalAnalysis;

public enum TokenType {
    IDENTIFIER,
    NUMBER,
    OPERATOR,
    PARENTHESIS,
    SEMICOLON,
    ASSIGNMENT,

    CURLYBRACKET,
    STRING,
    IF,
    LET,
    ELSE,
    WHILE,
    FOR,
    RETURN,


    UNKNOWN

}
